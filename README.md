# InventoryHandler API
```java
// in onEnable
new InventoryHandler(this);
```

# To open inventory
```java
// Create instance of object and invoke method open
new ExampleInventory().open(player);
```
